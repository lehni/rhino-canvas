Some Remarks:

- The UJAC-UI source code is included here since it requires 
  a fix for Mac OS X that cannot be done in a subclass. As
  soon as the bug is fixed in the UJAC project, the "fork" 
  will be removed and the jar file included instead.
  
- 
  